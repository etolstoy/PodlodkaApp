CREATE TABLE SqlEpisodeCategory (
  id TEXT NOT NULL,
  name TEXT NOT NULL,
  emoji TEXT NOT NULL
);

insertEpisodeCategory:
INSERT OR REPLACE INTO SqlEpisodeCategory(id, name, emoji)VALUES(?,?,?);

selectAll:
SELECT * FROM SqlEpisodeCategory;

selectById:
SELECT * FROM SqlEpisodeCategory WHERE id = ?;